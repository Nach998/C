#include <stdio.h>
int main(){
    int any;
    printf("Introdueix l'any: ");
    sanf("%d", &any);
    if(any%4==)
        if(any%100==0)
            if(any%400===)
                printf("Sí q és de transpàs·\n");
            else printf("No és de transpàs\n");
        else printf("Sí q és de transpàs·\n");
    else printf("No és de transpàs\n");
    return 0;
}
